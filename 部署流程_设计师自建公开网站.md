# 设计师自建公开访问网站 · 完整流程

> 本文档基于「灵感档案馆」项目的真实落地过程整理，是一份**可复用 SOP**。
> 适用场景：设计师/非专业开发，想把一个静态网页（作品集、灵感库、展示页）放到公网，让任何人都能通过网址访问。

---

## 一、先想清楚：一个网页能被别人访问，本质需要什么

别人能打开你的网页，本质只需满足一件事：

> **你的网页文件，必须放在一台「一直开机 + 公网能找到」的机器上。**

自己的电脑不满足（会关机、IP 会变、藏在路由器后面），所以正确路径是：
**把文件托管到一个公网随时可访问的平台 → 拿到一个网址 → 发给别人。**

"允许访问"这个动作本身不存在——公开平台上的网址，默认谁有链接谁就能看。

---

## 二、方案选型（为什么选 GitHub Pages）

| 方案 | 特点 | 适合谁 |
|------|------|--------|
| Vercel / Netlify | 拖文件即上线，零门槛 | 只想快速发布、不在意后续管理 |
| **GitHub Pages** | 免费、可长期管理、改完一推就更新 | **想长期维护、积累内容（本流程采用）** |
| 云厂商对象存储（OSS） | 国内访问快 | 受众在国内且在意速度 |

选 GitHub Pages 的关键理由：它支持「以后随时在线改内容、版本可追溯」，把网站变成一份**可长期生长的资产**，而不是一次性产物。

---

## 三、前置准备（一次性，以后不用再做）

1. **公网 GitHub 账号**（注意：不是公司内网 GitLab）
   - 用**个人邮箱**注册（公司邮箱离职即失效，私人资产不要绑公司）
   - 记好**用户名**——它会出现在最终网址里：`https://用户名.github.io/仓库名/`
2. **本机已安装 Git**（终端运行 `git --version` 能看到版本号即可）

---

## 四、核心步骤（共 6 步）

### Step 1 · 准备好网页文件
把网站做成纯静态文件，至少包含一个 `index.html`。
> 经验：把「数据」和「界面」分离——内容单独放一个 `data.js`，界面放 `index.html`。
> 这样以后新增内容只改 `data.js`（填空级别），永远不用碰界面代码。

### Step 2 · 本地初始化 Git 仓库
在项目目录下执行：
```bash
git init
git branch -m main          # 分支名改成 main，和 GitHub 默认对齐
git add .
git commit -m "init: 网站首次提交"
```

### Step 3 · 在 GitHub 上新建仓库（网页操作）
- 登录 github.com → 右上角 ➕ → New repository
- Repository name：填一个简单的名字（如 `gallery`）
- 选 **Public**（Pages 免费版要求公开仓库）
- README / .gitignore / license **三个都不要勾**（本地已有文件，勾了会冲突）
- Create repository → 复制得到的仓库地址 `https://github.com/用户名/仓库名.git`

### Step 4 · 关联远程仓库
```bash
git remote add origin https://github.com/用户名/仓库名.git
```

### Step 5 · 认证并推送代码
GitHub 已不支持账号密码推送，需用 **Personal Access Token（PAT）** 当密码：
1. 打开 github.com/settings/tokens → Generate new token (classic)
2. 备注随意，有效期按需，**权限只勾 `repo`**
3. 生成后复制那串 `ghp_` 开头的令牌（**只显示一次**）
4. 推送（令牌临时拼进 URL，不写入本地配置）：
```bash
git push https://用户名:令牌@github.com/用户名/仓库名.git main
# 推完把远程地址恢复成不含令牌的干净版本：
git remote set-url origin https://github.com/用户名/仓库名.git
```

### Step 6 · 开启 GitHub Pages（网页操作）
- 进入仓库 Settings → Pages
- Source 选 **Deploy from a branch**
- Branch 选 **main**，文件夹 **/ (root)** → Save
- 等 1~2 分钟，页面显示：`Your site is live at https://用户名.github.io/仓库名/`

**🎉 这个网址发给任何人，任何设备都能打开。**

---

## 五、以后怎么更新内容（日常维护）

新增/修改内容后，在项目目录跑三行：
```bash
git add .
git commit -m "更新内容"
git push
```
推完等一两分钟，线上网站自动更新。（令牌已缓存，一般无需重输。）

---

## 六、安全提醒

- Personal Access Token 相当于临时密码：**用完即焚**。
  不再需要命令行推送时，回 github.com/settings/tokens 把它 **Revoke（吊销）**。
- 仓库是 Public 的：**不要把任何敏感信息**（密钥、私人数据）放进去。
- 令牌设了有效期，到期自动失效，需要时再生成新的即可。

---

## 七、本质回顾（这套流程在解决什么）

| 表面动作 | 本质 |
|----------|------|
| 选 GitHub Pages | 把网站当作**可长期生长的资产**，而非一次性产物 |
| 数据/界面分离 | 把维护成本压到「填空级别」，让非开发者也能持续更新 |
| 公开仓库 + 公网平台 | 「允许访问」不需要额外授权——**公开即可访问** |
| Token 用完即焚 | 在「方便」和「安全」之间取最小够用的权限 |

> 一句话：建网站不难，难的是想清楚**你要的不是一个网站，而是「东西能被人随时看到」这件事被满足**——
> 选对托管方式，比写代码更重要。
